// Calendar state is managed by CalendarContext — this re-exports for convenience
// Additional UI-only calendar state can go here if needed
export { useCalendar } from '../../data/context/calendar-context'
